apiVersion: v1
kind: Service
metadata:
  name: application
  labels:
    app: employees
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: employees
    nodePort: 30000